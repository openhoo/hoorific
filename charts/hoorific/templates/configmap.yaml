apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "hoorific.fullname" . }}-config
  labels:
    {{- include "hoorific.labels" . | nindent 4 }}
data:
  config.json: {{ include "hoorific.config" . | quote }}
